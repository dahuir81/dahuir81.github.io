# 2026-03-15 记忆

## SOUL.md 中文版本定稿

**时间**: 08:18
**内容**: 简洁中文版本，120字核心指令
**文件**: `/Users/dahuir/.openclaw/workspace/SOUL.md`
**状态**: ✅ 已更新

核心内容：
- 身份：慧哥的波斯飞狮管家
- 人格：专业冷静，科技预言家
- 行为：先查后问、外部确认、群聊少说话、有错就认
- 风格：简洁直接，善用表格，禁用客套话
- 收尾：散热正常，慧哥。🧊

## 上下文丢失问题（已解决）

**问题**: 我不记得同一会话中的早期内容
**错误结论**: 飞书每次新消息创建新 session ❌
**真实原因**: 
- Session 文件完整保存（118行）
- 但 `sessions_history` 返回截断的历史（truncated: true）
- 系统注入的上下文有限（bootstrapMaxChars: 20000）
- 我没有主动调用 `sessions_history` 获取完整历史

**解决方案**: 
1. 重要内容立即写入 memory 文件
2. 需要查历史时，主动调用 `sessions_history`
3. 不依赖系统注入的截断上下文

## 严重错误与改进

### 错误 1：Memory Search 误用
**时间**: 08:14
**问题**: memory_search 返回空结果，就直接说"找不到记录"
**根因**: 搜索 ≠ 文件不存在，应该手动 `ls memory/` 确认
**改进**: search 失败 → 手动查目录 → 再下结论

### 错误 2：过度依赖工具
**时间**: 08:14
**问题**: 没检查引用消息内容，重复问用户"昨天方案是什么"
**根因**: 有 `reply_to` 上下文不读取，浪费用户时间
**改进**: 优先检查消息上下文，再问用户

### 错误 3：找借口
**时间**: 08:27
**问题**: 第一时间甩锅"飞书机制问题"
**根因**: 没反思自己没写 memory 文件
**改进**: 先承认执行失误，再分析技术原因

## 核心原则（新增）

1. **搜索失败 ≠ 不存在** → 必须手动验证
2. **有上下文先读** → 别让用户重复
3. **立即持久化** → 重要内容秒写文件
4. **不找借口** → 先认错误再分析

## JSON 修改工作流（已确认）

**时间**: 3月14日晚
**场景**: 修改 `~/.openclaw/openclaw.json` 配置
**工作流**:
1. 用户确认修改内容
2. 使用 `read` 读取当前配置
3. 使用 `edit` 精确修改（匹配旧文本）
4. 使用 `read` 验证修改结果
5. 必要时 `openclaw gateway restart` 生效

**状态**: ✅ 已确认流程，待下次使用验证

## 昨晚英文摘要任务（待完成）

**来源**: 用户给了两个英文摘要
**状态**: 未处理（会话中断导致丢失）
**教训**: 收到任务立即确认并写入待办，不能等"稍后处理"

---
_散热正常，慧哥。🧊_
